namespace CSharpAwesome;

public class FileScopedNamespaces
{
    // There is a fixer so you can switch back and forth
    // with Ctl .

    // It's nice to have less indentation, but it changes
    // every line in each file where it is applied. So
    // it can wreak havoc on repo management.
}
